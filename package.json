{
  "name": "cardify",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit && tsc --noEmit -p scripts/tsconfig.json",
    "mint": "tsx scripts/mint-url.ts"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250523.0",
    "@types/node": "^25.9.1",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "wrangler": "^3.80.0"
  }
}
